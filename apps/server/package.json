{
  "name": "notiftool-server",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@orpc/server": "^1.6.6",
    "shared": "workspace:*",
    "hono": "^4.8.3",
    "zod": "^3.25.67"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
